import Neat from "neat-components";

const gridSettings = Neat({
  columns: 12,
  gutter: "30px",
  color: "#d9f9f9", // used for grid overlay
});

const THEME = {
  ...gridSettings,
};

export default THEME;
